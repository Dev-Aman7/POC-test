import React from 'react';
import { Typography, Stack, Container } from '@mui/material';
import Layout from '../../components/Layout';

function Home() {
  return <Layout>chow chow</Layout>;
}

export default Home;
